apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "observability-stack.otelCollector.name" . }}-config
  labels:
    {{- include "observability-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: otel-collector
data:
  config.yaml: |
{{ .Files.Get "files/otel-collector-config.yaml" | indent 4 }}
