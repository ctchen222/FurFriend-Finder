apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "observability-stack.tempo.name" . }}-config
  labels:
    {{- include "observability-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: tempo
data:
  tempo.yaml: |
{{ .Files.Get "files/tempo.yaml" | indent 4 }}
